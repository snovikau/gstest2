import React from 'react';

/**
 * Implements Header logic and styles. Can be extended to implement Header-levels
 * @param title
 * @returns {*}
 * @constructor
 */
const Header = ({title}) => <h3>{title}</h3>;

export default Header;